# 트러블슈팅
## API 키 오류
"Invalid API key" 오류가 발생하면 환경 변수 OPENAI_API_KEY 를 확인한다.
API 키는 "sk-" 로 시작해야 한다.

## 빌드 실패
Gradle 빌드가 실패하면 Java 21 이상이 설치되어 있는지 확인한다.
java -version 명령으로 버전을 확인할 수 있다.

## 임베딩 차원 불일치
벡터 저장소에 저장된 벡터와 검색 벡터의 차원이 다르면 오류가 발생한다.
동일한 임베딩 모델을 사용하고 있는지 확인한다.
